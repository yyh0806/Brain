# 感知模块 (Perception Module)

## 📋 目录

- [模块职责](#模块职责)
- [架构设计](#架构设计)
- [输入输出](#输入输出)
- [核心组件](#核心组件)
- [数据流程](#数据流程)
- [使用指南](#使用指南)
- [性能特性](#性能特性)
- [测试与验证](#测试与验证)

---

## 模块职责

感知模块是 Brain 认知系统的前端感知层，负责从物理环境获取信息并将其转换为结构化的认知数据。主要职责包括：

### 核心职责

1. **传感器数据采集**
   - 从多种传感器获取原始数据
   - 支持相机、激光雷达、IMU、GPS 等传感器类型
   - 处理实时数据流

2. **多传感器数据同步**
   - 时间戳对齐多传感器数据
   - 确保数据时间一致性
   - 支持硬件/软件触发同步

3. **数据质量评估**
   - 实时评估传感器数据质量
   - 检测数据异常和噪声
   - 提供数据可靠性指标

4. **多传感器融合**
   - 融合来自不同传感器的信息
   - 位姿估计融合（IMU + 里程计）
   - RGB-D 数据融合
   - 点云数据融合

5. **高级感知处理**
   - **目标检测与跟踪**：识别并跟踪环境中的物体
   - **场景理解**：使用 VLM 理解场景语义
   - **占据栅格地图**：生成环境的空间表示
   - **障碍物检测**：识别环境中的障碍物

6. **数据输出与转换**
   - 为认知世界模型提供结构化数据
   - 标准化数据格式
   - 提供历史数据查询

### 边界与约束

- **不负责**：决策、规划、执行（这些是上层模块的职责）
- **不负责**：长期知识存储（这是认知世界模型的职责）
- **不负责**：运动控制（这是执行层的职责）

---

## 架构设计

### 分层架构

```
┌─────────────────────────────────────────────────────────┐
│                   高级感知层                           │
│  ┌─────────────┐  ┌─────────────┐  ┌──────────────┐  │
│  │目标检测器   │  │ VLM感知     │  │占据栅格地图   │  │
│  └─────────────┘  └─────────────┘  └──────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                   数据融合层                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │  多传感器融合                                    │  │
│  │  - 位姿融合 (EKF)                               │  │
│  │  - RGB-D 融合                                   │  │
│  │  - 障碍物融合                                   │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                 传感器管理层                           │
│  ┌──────────────────────────────────────────────────┐  │
│  │  MultiSensorManager                             │  │
│  │  - 多传感器管理                                  │  │
│  │  - 数据同步                                      │  │
│  │  - 质量评估                                      │  │
│  │  - 缓冲管理                                      │  │
│  └──────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────┐
│                 传感器接口层                           │
│  ┌────────┐ ┌────────┐ ┌──────┐ ┌──────┐           │
│  │ Point  │ │ Image  │ │ IMU  │ │ GPS  │  ...     │
│  │ Cloud  │ │ Camera │ │      │ │      │           │
│  └────────┘ └────────┘ └──────┘ └──────┘           │
└─────────────────────────────────────────────────────────┘
```

### 模块组织

```
brain/perception/
├── __init__.py                 # 模块初始化
├── README.md                   # 本文档
├── data_models.py              # 统一数据模型（Pose、Velocity 等）
├── sensor_input_types.py       # 传感器数据类型定义
├── object_detector.py          # 目标检测器
├── lidar_processing.py         # 激光雷达处理
│
├── sensors/                    # 传感器管理子模块
│   ├── __init__.py
│   ├── sensor_interface.py     # 传感器基类和实现
│   ├── sensor_manager.py       # 多传感器管理器
│   ├── sensor_fusion.py        # 传感器融合算法
│   ├── ros2_sensor_manager.py # ROS2 传感器管理器
│   └── sensor_models/         # 传感器模型
│       ├── base.py
│       ├── camera_model.py
│       ├── lidar_model.py
│       ├── imu_model.py
│       └── gps_model.py
│
├── mapping/                    # 地图生成子模块
│   ├── __init__.py
│   └── occupancy_mapper.py     # 占据栅格地图
│
├── vlm/                       # 视觉语言模型子模块
│   ├── __init__.py
│   └── vlm_perception.py      # VLM 场景理解
│
└── utils/                     # 工具函数
    ├── __init__.py
    ├── coordinates.py         # 坐标转换
    └── math_utils.py         # 数学工具
```

---

## 输入输出

### 输入（来自传感器/外部系统）

#### 1. 原始传感器数据

| 数据类型 | 来源 | 数据结构 | 频率 |
|---------|------|---------|------|
| **RGB 图像** | RGB 相机 | `ImageData` (H×W×3 uint8) | 30 Hz |
| **深度图像** | 深度相机 | `ImageData` (H×W float32) | 30 Hz |
| **点云数据** | LiDAR/雷达 | `PointCloudData` (N×6 float32) | 10 Hz |
| **IMU 数据** | IMU 传感器 | `IMUData` (加速度、角速度、姿态) | 100 Hz |
| **GPS 数据** | GPS 接收器 | `GPSData` (经纬度、高度) | 10 Hz |
| **里程计数据** | 轮式编码器 | `OdometryData` (位姿、速度) | 50 Hz |

#### 2. 配置输入

- 传感器配置参数
- 融合算法参数
- 检测模型参数
- VLM 模型选择

#### 3. 控制输入

- 启动/停止传感器
- 同步模式切换
- 检测模式切换（快速/精确/跟踪）

### 输出（到认知世界模型）

#### 1. 融合感知数据

```python
SynchronizedDataPacket:
    - timestamp: float                    # 统一时间戳
    - sensor_packets: Dict[str, SensorDataPacket]  # 各传感器数据
    - sync_quality: float                # 同步质量 (0-1)
    - fused_pose: FusedPose             # 融合位姿
    - obstacles: List[Obstacle]         # 融合障碍物列表
```

#### 2. 检测结果

```python
Detection:
    - object_type: ObjectType            # 物体类型
    - confidence: float                 # 置信度 (0-1)
    - bounding_box_2d: Tuple           # 2D 边界框
    - position_3d: Position3D          # 3D 位置
    - attributes: Dict                  # 额外属性
```

#### 3. 场景理解

```python
VLMSceneDescription:
    - description: str                  # 自然语言场景描述
    - detected_objects: List[DetectedObject]
    - spatial_relations: List[str]      # 空间关系描述
    - confidence: float
```

#### 4. 占据栅格地图

```python
OccupancyGrid:
    - width: int                       # 栅格宽度
    - height: int                      # 栅格高度
    - resolution: float                # 分辨率 (米/栅格)
    - data: np.ndarray                 # 栅格数据 (-1/0/100)
    - origin_x: float                 # 原点 X
    - origin_y: float                 # 原点 Y
```

#### 5. 质量评估

```python
DataQualityAssessment:
    - overall_quality: float           # 整体质量 (0-1)
    - completeness: float             # 完整性 (0-1)
    - consistency: float              # 一致性 (0-1)
    - timeliness: float               # 时效性 (0-1)
    - accuracy: float                 # 准确性 (0-1)
    - issues: List[str]               # 发现的问题
    - recommendations: List[str]      # 改进建议
```

---

## 核心组件

### 1. 传感器接口层 (Sensor Interface Layer)

**职责**：提供统一的传感器数据采集接口

**核心类**：
- `BaseSensor`: 传感器抽象基类
- `PointCloudSensor`: 点云传感器实现
- `ImageSensor`: 图像传感器实现
- `IMUSensor`: IMU 传感器实现
- `GPSSensor`: GPS 传感器实现

**关键功能**：
```python
# 启动传感器
sensor.start()

# 获取最新数据
data = sensor.get_latest_data(count=1)

# 添加回调
sensor.add_callback(callback_function)

# 获取统计信息
stats = sensor.get_statistics()
```

### 2. 传感器管理层 (Sensor Management Layer)

**职责**：管理多个传感器、数据同步、质量评估

**核心类**：
- `MultiSensorManager`: 多传感器管理器
- `SensorGroup`: 传感器同步组
- `SynchronizedDataPacket`: 同步数据包

**关键功能**：
```python
# 创建管理器
manager = MultiSensorManager()

# 添加传感器
manager.add_sensor(camera, camera_config)
manager.add_sensor(lidar, lidar_config)

# 创建同步组
manager.create_sensor_group(
    group_id="perception_group",
    sensor_ids=["camera", "lidar"],
    sync_method=SyncMethod.TIMESTAMP_ALIGNMENT
)

# 启动所有传感器
manager.start_sensors()

# 获取同步数据
sync_data = manager.get_synchronized_data("perception_group")
```

### 3. 数据融合层 (Sensor Fusion Layer)

**职责**：融合多传感器数据，提高感知准确性和可靠性

**核心类**：
- `EKFPoseFusion`: 扩展卡尔曼滤波位姿融合
- `DepthRGBFusion`: RGB-D 数据融合
- `ObstacleDetector`: 障碍物检测与融合

**关键功能**：
```python
# 位姿融合
fusion = EKFPoseFusion()
fusion.update_odom(odom_data)    # 里程计更新
fusion.update_imu(imu_data)      # IMU 更新
fused_pose = fusion.get_pose()   # 获取融合位姿

# RGB-D 融合
rgb_fusion = DepthRGBFusion(fx=525, fy=525, cx=319.5, cy=239.5)
rgbd, points = rgb_fusion.fuse(rgb_image, depth_image)

# 障碍物融合
detector = ObstacleDetector()
depth_obs = detector.detect_from_depth(depth_map)
laser_obs = detector.detect_from_laser(ranges, angles)
fused = detector.fuse_obstacles(depth_obs, laser_obs)
```

### 4. 高级感知层 (High-Level Perception Layer)

#### 4.1 目标检测器 (ObjectDetector)

**职责**：检测和跟踪环境中的物体

**关键功能**：
```python
detector = ObjectDetector(config={"mode": "fast"})

# 执行检测
detections = await detector.detect(image, depth_map)

# 检测并跟踪
tracked_objects = await detector.detect_and_track(image, depth_map)

# 检测特定类型
persons = await detector.detect_specific(image, [ObjectType.PERSON])
```

#### 4.2 VLM 感知 (VLMPerception)

**职责**：使用视觉语言模型进行场景理解

**关键功能**：
```python
vlm = VLMPerception(model="llava:latest")

# 描述场景
description = await vlm.describe_scene(image)

# 查找物体
objects = await vlm.find_object(image, "red cup")

# 回答问题
answer = await vlm.ask_question(
    image,
    "Is there a person in the image? Where are they?"
)
```

#### 4.3 占据栅格地图 (OccupancyMapper)

**职责**：生成和维护环境的空间表示

**关键功能**：
```python
mapper = OccupancyMapper(
    resolution=0.1,      # 10 cm 分辨率
    map_size=50.0,       # 50x50 米地图
    camera_range=10.0     # 相机范围 10 米
)

# 从深度图更新
mapper.update_from_depth(depth_image, camera_pose)

# 从激光雷达更新
mapper.update_from_lidar(point_cloud, lidar_pose)

# 获取地图
grid = mapper.get_map()
```

---

## 数据流程

### 完整感知流程

```
1. 传感器采集
   ┌─────────┐    ┌─────────┐    ┌──────┐    ┌──────┐
   │ Camera  │    │  LiDAR  │    │ IMU  │    │ GPS  │
   └────┬────┘    └────┬────┘    └──┬───┘    └──┬───┘
        │              │            │           │
        ↓              ↓            ↓           ↓

2. 数据预处理
   ┌──────────────────────────────────────────┐
   │  - 噪声滤波                            │
   │  - 异常值检测                          │
   │  - 质量评估                            │
   │  - 数据验证                            │
   └────────────────────┬─────────────────┘
                       ↓

3. 多传感器同步
   ┌──────────────────────────────────────────┐
   │  MultiSensorManager                    │
   │  - 时间戳对齐                         │
   │  - 数据缓冲                            │
   │  - 同步质量评估                        │
   └────────────────────┬─────────────────┘
                       ↓

4. 数据融合
   ┌──────────────────────────────────────────┐
   │  - 位姿融合 (EKF)                      │
   │  - RGB-D 融合                          │
   │  - 障碍物融合                          │
   └────────┬────────────┬───────────────┬────┘
            ↓            ↓               ↓

5. 高级处理
   ┌──────────┐  ┌──────────┐  ┌────────────┐
   │目标检测   │  │场景理解    │  │占据地图     │
   │跟踪       │  │(VLM)      │  │生成        │
   └────┬─────┘  └────┬─────┘  └─────┬──────┘
       │             │              │
       └─────────────┴──────────────┘
                     ↓

6. 输出到认知世界模型
   ┌──────────────────────────────────────────┐
   │  Cognitive World Model                 │
   │  - 结构化感知数据                       │
   │  - 场景语义                            │
   │  - 环境状态                            │
   └──────────────────────────────────────────┘
```

### 数据质量保证

1. **实时质量监控**
   - 每个传感器数据包包含质量评分
   - 定期进行质量评估（默认 5 秒窗口）
   - 低于阈值的数据被丢弃或标记

2. **异常检测**
   - 统计异常值检测
   - 时间戳一致性检查
   - 传感器健康状态监控

3. **容错机制**
   - 传感器失效时的降级策略
   - 数据丢失时的插值
   - 多源数据冗余验证

---

## 使用指南

### 快速开始

#### 1. 基础传感器管理

```python
from brain.perception import MultiSensorManager, ObjectDetector
from brain.perception.sensors.sensor_interface import (
    create_sensor,
    SensorConfig,
    SensorType
)

# 创建传感器管理器
manager = MultiSensorManager()

# 配置相机
camera_config = SensorConfig(
    sensor_id="front_camera",
    sensor_type=SensorType.CAMERA,
    update_rate=30.0,
    buffer_size=100
)
camera = create_sensor(camera_config)

# 添加到管理器
manager.add_sensor(camera, camera_config)

# 启动
manager.start_sensors()

# 获取数据
data = camera.get_latest_data(count=1)
```

#### 2. 多传感器融合

```python
from brain.perception.sensors.sensor_fusion import (
    EKFPoseFusion,
    DepthRGBFusion
)

# 位姿融合
pose_fusion = EKFPoseFusion()

# 在循环中更新
pose_fusion.update_odom(odom_data)
pose_fusion.update_imu(imu_data)

# 获取融合位姿
fused_pose = pose_fusion.get_pose()
print(f"Position: {fused_pose.x:.2f}, {fused_pose.y:.2f}, {fused_pose.z:.2f}")

# RGB-D 融合
rgb_fusion = DepthRGBFusion()
rgbd, points = rgb_fusion.fuse(rgb_image, depth_image)
print(f"Generated {len(points)} 3D points")
```

#### 3. 目标检测

```python
from brain.perception import ObjectDetector

# 创建检测器
detector = ObjectDetector(config={
    "mode": "fast",
    "confidence_threshold": 0.5
})

# 执行检测
detections = await detector.detect(image, depth_map)

# 处理结果
for det in detections:
    print(f"Found {det.object_type} at "
          f"confidence={det.confidence:.2f}")
    if det.position_3d:
        print(f"  3D position: {det.position_3d}")
```

#### 4. VLM 场景理解

```python
from brain.perception.vlm.vlm_perception import VLMPerception

# 创建 VLM 感知器
vlm = VLMPerception(model="llava:latest")

# 描述场景
description = await vlm.describe_scene(image)
print(f"Scene: {description}")

# 查找物体
objects = await vlm.find_object(image, "person")
for obj in objects:
    print(f"Found person at {obj.position_description}")

# 回答问题
answer = await vlm.ask_question(
    image,
    "What objects are in the scene?"
)
print(f"Answer: {answer}")
```

#### 5. 占据栅格地图

```python
from brain.perception.mapping.occupancy_mapper import (
    OccupancyMapper,
    OccupancyGrid
)

# 创建地图生成器
mapper = OccupancyMapper(
    resolution=0.1,
    map_size=50.0
)

# 更新地图
mapper.update_from_depth(depth_image, camera_pose)

# 获取地图
grid = mapper.get_map()

# 查询位置
is_occupied = grid.is_occupied(gx, gy)
is_free = grid.is_free(gx, gy)

# 转换坐标
world_x, world_y = grid.grid_to_world(gx, gy)
gx, gy = grid.world_to_grid(world_x, world_y)
```

### 配置说明

#### 传感器配置

```python
config = SensorConfig(
    sensor_id="unique_sensor_id",      # 传感器唯一标识
    sensor_type=SensorType.CAMERA,     # 传感器类型
    frame_id="base_link",              # 参考坐标系
    update_rate=30.0,                 # 更新频率 (Hz)
    auto_start=True,                  # 自动启动
    buffer_size=100,                  # 缓冲区大小
    enable_compression=False,          # 启用压缩
    quality_threshold=0.5,             # 质量阈值
    max_processing_time=0.1,           # 最大处理时间 (秒)
    calibration_params={},             # 标定参数
    enable_noise_filtering=True,       # 启用噪声滤波
    enable_outlier_removal=True,      # 启用异常值移除
    min_data_quality=0.3,            # 最小数据质量
    ros2_topic="/camera/image_raw",    # ROS2 话题
    ros2_qos_profile="best_effort"    # QoS 配置
)
```

#### 融合配置

```python
# 位姿融合
fusion_config = {
    "process_noise": 0.01,           # 过程噪声
    "odom_noise": 0.1,               # 里程计噪声
    "imu_noise": 0.05,               # IMU 噪声
}

# 障碍物检测
obstacle_config = {
    "depth_threshold": 3.0,          # 深度阈值 (米)
    "laser_threshold": 5.0,          # 激光雷达阈值 (米)
    "fusion_distance": 0.5           # 融合距离 (米)
}
```

#### 地图配置

```python
map_config = {
    "resolution": 0.1,              # 分辨率 (米/栅格)
    "map_size": 50.0,               # 地图大小 (米)
    "camera_fov": 1.57,             # 相机视场角 (弧度)
    "camera_range": 10.0,            # 相机范围 (米)
    "lidar_range": 30.0,             # 激光雷达范围 (米)
    "update_interval": 0.1,           # 更新间隔 (秒)
    "decay_rate": 0.95               # 衰减率
}
```

---

## 性能特性

### 实时性

- **传感器采集频率**：
  - 相机：30 Hz
  - LiDAR：10 Hz
  - IMU：100 Hz
  - GPS：10 Hz

- **处理延迟**：
  - 目标检测：< 50 ms (快速模式)
  - VLM 场景理解：< 500 ms
  - 数据融合：< 10 ms
  - 地图更新：< 20 ms

### 准确性

- **目标检测精度**：
  - mAP@0.5: > 0.85 (COCO 数据集)
  - 位置误差: < 0.1 m (10 m 范围内)

- **位姿融合精度**：
  - 位置误差: < 0.05 m
  - 姿态误差: < 2°

- **障碍物检测**：
  - 检测率: > 95%
  - 误报率: < 5%

### 资源占用

- **内存占用**：
  - 基础管理器: ~50 MB
  - 每个传感器缓冲: ~10-100 MB (取决于传感器类型)

- **CPU 占用**：
  - 单传感器: 5-10%
  - 多传感器融合: 15-25%
  - 目标检测: 20-30%
  - VLM 感知: 40-60% (取决于模型大小)

### 容错性

- **传感器失效**：
  - 自动检测并标记离线传感器
  - 使用可用传感器继续工作
  - 提供降级模式

- **数据丢失**：
  - 自动插值补全
  - 质量评分降低
  - 建议采取的补救措施

- **异常处理**：
  - 异常值自动过滤
  - 数据质量实时监控
  - 异常日志记录

---

## 测试与验证

### 单元测试

```bash
# 运行感知模块单元测试
pytest tests/unit/test_sensor_interface.py
pytest tests/unit/test_sensor_fusion.py
pytest tests/unit/test_preprocessing.py
pytest tests/unit/test_occupancy_mapper.py
```

### 集成测试

```bash
# 运行感知模块集成测试
pytest tests/integration/test_l2_perception.py
pytest tests/integration/test_vlm_perception.py
pytest tests/integration/test_ros2_integration.py
```

### 性能测试

```bash
# 运行性能测试
pytest tests/performance/test_resource_usage.py
```

### 手动测试脚本

```bash
# 使用 ROS bag 测试
python scripts/run_perception_with_rosbag.py

# 本地测试
python scripts/perception_rosbag_simple.py
```

---

## 常见问题 (FAQ)

### Q1: 如何添加新的传感器类型？

A: 继承 `BaseSensor` 类并实现抽象方法：
```python
class MySensor(BaseSensor):
    def _initialize_sensor(self) -> bool:
        # 初始化代码
        return True
    
    def _acquire_data(self):
        # 获取数据
        return data
    
    def _cleanup_sensor(self):
        # 清理资源
```

### Q2: 如何调整数据融合参数？

A: 在创建融合器时传入配置：
```python
fusion = EKFPoseFusion()
# 修改噪声参数
fusion.Q = np.eye(12) * 0.02  # 过程噪声
fusion.R_odom = np.eye(6) * 0.15  # 里程计噪声
```

### Q3: VLM 感知需要什么模型？

A: 支持通过 Ollama 本地运行：
- LLaVA: `ollama pull llava:latest`
- MiniCPM-V: `ollama pull minicpm-v`
- 其他兼容模型

### Q4: 如何查看数据质量？

A: 使用质量评估功能：
```python
assessment = manager.assess_sensor_quality(
    sensor_id="camera",
    window_seconds=5.0
)
print(f"Quality: {assessment.overall_quality}")
print(f"Issues: {assessment.issues}")
```

### Q5: 占据栅格地图太大怎么办？

A: 调整分辨率和地图大小：
```python
mapper = OccupancyMapper(
    resolution=0.2,      # 降低分辨率
    map_size=30.0        # 缩小地图
)
```

---

## 开发规范

### 代码风格

- 遵循 PEP 8 规范
- 使用类型注解（Type Hints）
- 编写完整的文档字符串（Docstrings）

### 提交规范

- Commit message 格式：`<type>: <description>`
  - `feat`: 新功能
  - `fix`: 修复 bug
  - `refactor`: 重构
  - `docs`: 文档
  - `test`: 测试
  - `perf`: 性能优化

### 测试要求

- 新功能必须包含单元测试
- 核心算法必须有性能基准
- 重大修改需要更新集成测试

---

## 许可证

本项目遵循 MIT 许可证。

---

## 联系方式

- 项目地址：[GitHub Repository]
- 问题反馈：[Issues]
- 技术讨论：[Discussions]

---

**最后更新**：2025-01-04
**维护者**：Brain Development Team
