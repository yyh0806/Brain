# Brain - 无人系统任务分解大脑
# 依赖清单

# 核心依赖
pydantic>=2.5.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
# asyncio-compat>=0.1.0  # 已移除，此包不存在

# LLM接口
openai>=1.3.0
anthropic>=0.7.0
httpx>=0.25.0
ollama>=0.3.0  # Ollama本地大模型

# 状态机与任务调度
transitions>=0.9.0
apscheduler>=3.10.0

# 日志与监控
loguru>=0.7.2
prometheus-client>=0.19.0

# 数据处理
numpy>=1.24.0
scipy>=1.11.0

# 图像处理
Pillow>=10.0.0
opencv-python>=4.8.0  # 可选，用于图像处理
scikit-image>=0.20.0

# VLM/YOLO
ultralytics>=8.0.0  # YOLO目标检测
torch>=2.0.0
torchvision>=0.15.0

# 点云处理
open3d>=0.17.0

# 机器学习
scikit-learn>=1.3.0

# 传感器数据处理
numpy-quaternion>=2022.4.0

# 通信协议
pyzmq>=25.1.0
protobuf>=4.24.0
grpcio>=1.59.0

# 地理空间计算
pyproj>=3.6.0
shapely>=2.0.0

# 可视化调试 (可选)
matplotlib>=3.8.0
networkx>=3.2.0

# 测试
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0

# 类型检查
mypy>=1.7.0

# ROS2相关 (需要单独安装ROS2环境后才能使用)
# rclpy  # 通过ROS2安装
# geometry_msgs
# sensor_msgs
# nav_msgs
# cv_bridge

# Isaac Sim仿真环境 (需要NVIDIA GPU和兼容环境)
# isaacsim>=4.0.0  # Isaac Sim核心包，需要NVIDIA GPU支持
# 注意：isaacsim需要以下系统要求：
# - Python 3.10 (Isaac Sim 4.x) 或 Python 3.11 (Isaac Sim 5.x)
# - NVIDIA GPU with VOLTA架构或更新版本
# - 4GB+ VRAM
# - GLIBC 2.35+
# 安装前请运行: ldd --version 检查GLIBC版本

# Isaac Sim扩展和工具
# isaaclab  # Isaac Lab框架，提供更高级的机器人仿真接口
# omni.kit.primitive.core  # 3D基础图元
# omni.kit.loop-isaac  # Isaac仿真循环

# 可选的Isaac Sim依赖
# carb  # Carbonite框架
# usd-core  # Universal Scene Description支持
# pybind11  # Python C++绑定

# 替代仿真环境 (如果Isaac Sim环境不满足要求)
# pybullet  # 轻量级物理仿真，作为备选方案

